# 앞으로 할 일

이 저장소에서 아직 못 한 것들을 정리해 둠.
시간이 없어 손대지 못한 부분이 대부분임. 순서는 중요도 순.

---

## 1. 시계열 해석 연동

`load_8760.csv`, `pv_8760.csv`는 만들어 뒀으나 아직 코드에 붙이지 않음.
현재는 스냅샷만 돌아감.

- [ ] `network_model.py`에 LoadShape 정의 추가
- [ ] `Set mode=yearly` 경로 만들기
- [ ] 8760시간 결과를 지표로 압축 — 역조류 발생 시간수, 전압 1.05pu 초과 시간수, 최대 역조류량

주의: 8760시간이면 스냅샷보다 훨씬 느림. 웹 API로 노출하면 타임아웃 발생.
CLI 배치로 돌리고 결과만 CSV로 떨구는 쪽이 맞을 듯.

스냅샷 대비 시계열로 수용용량이 얼마나 늘어나는지 비교해 보면 좋겠음.

---

## 2. 계통 생성기

현재 피더 2개는 손으로 만듦. 늘리려면 매번 같은 작업 반복해야 함.

- [ ] 파라미터로 계통을 생성하는 스크립트
      (피더 수, 구간 수, 분기 깊이, 부하 밀도, 전선 규격 배분)
- [ ] 생성 결과를 CIM XML로 출력 → Neo4j 적재까지 자동화

45MVA 뱅크에 5회선, 20회선 같은 걸 명령 하나로 만들 수 있어야
뱅크 단위 검토가 가능해짐.

---

## 3. 부하 데이터 현실화

현재 부하값은 전부 임의값. 조류계산은 돌아가나 결과에 의미를 부여하기 어려움.

- [ ] 배전설계기준 기반으로 부하 밀도 산정
- [ ] 말단으로 갈수록 부하밀도 감소하는 패턴 반영
      (현재는 균등 배분이라 말단 전압강하가 실제보다 낮게 나옴)
- [ ] 단상 부하 비율 반영

---

## 4. API 정리

- [ ] `/api/sim/voltage`의 PV 50% 고정을 파라미터로 빼기
      (왜 50%인지 근거 없고 최악 조건도 아님)
- [ ] `/api/sim/reverse`에 부하 조건 명시 — 최소부하인지 최대부하인지 빠져 있음
- [ ] 수용용량 탐색 함수 추가
      `find_max_capacity(feeder, section)` — 이진 탐색으로 한계 용량 찾기

---

## 5. 판정 로직 분리

현재는 조류계산 결과가 그대로 나옴. 판정은 사람이 해야 함.
이걸 코드로 빼두면 나중에 쓸모가 많음.

- [ ] 판정 결과를 구조화된 JSON으로 반환

```json
{
  "verdict": "불가",
  "reason_code": "OVERVOLTAGE",
  "worst_bus": "B_14",
  "worst_voltage_pu": 1.062,
  "limit_pu": 1.05,
  "max_acceptable_kw": 2100,
  "condition": "최소부하 + PV 100%"
}
```

`reason_code`는 과전압, 열용량 초과, 역조류 한계 초과 정도로 나누면 될 듯.

---

## 6. 자연어 인터페이스 (POC)

여기까지 오면 쓸모가 생김.

현재는 조류계산을 돌리려면 OpenDSS 문법과 계통 데이터 구조를 알아야 함.
현장에서 문의가 와도 즉답을 못 하는 이유.

목표는 이런 형태.

```
"8번 구간에 3MW 태양광 넣을 수 있나?"

"어렵습니다. 최소부하 조건에서 B_14 모선 전압이 1.062pu까지 올라가
 기준(1.05pu)을 초과합니다. 같은 위치라면 2.1MW까지 가능합니다."
```

- [ ] 자연어에서 파라미터 추출 (피더, 구간, 용량, 부하 조건)
- [ ] Tool Use로 시뮬레이션 함수 호출
- [ ] 5번의 JSON을 자연어로 서술
- [ ] 멀티턴 — "그럼 얼마까지?"에서 앞 맥락 유지

### 반드시 지킬 것

**계산은 코드가, 서술은 LLM이.**

원시 결과를 던져주고 "분석해줘" 하면 없는 숫자를 만들어냄.
전력공학 용어와 함께 나오면 그럴듯해서 구분이 안 됨.

```
OpenDSS 결과 → 판정 로직(코드) → JSON → LLM(서술만)
```

프롬프트에 "주어진 JSON 값만 쓰고 새로 계산하지 말 것"을 명시하고,
지켜지는지 검증 코드로 확인해야 함.
답변에 나온 숫자가 JSON에 없는 값이면 걸러내는 식.

**실행 전 확인 단계.**
파라미터 추출이 틀려도 조용히 넘어감. 엉뚱한 피더로 계산해놓고
그럴듯하게 답할 수 있음.

```
"DL2 피더 8번 구간, 3MW, 최소부하 조건으로 검토하겠습니다."
```

이 한 줄을 실행 전에 보여주는 것만으로 대부분 걸러짐.

**근거 포함.**
"불가합니다"만 나오면 아무도 안 믿음.
어느 모선에서 몇 pu, 기준이 얼마, 얼마까지는 가능한지. 이 세 개는 항상 포함.

### 도구 목록 (검토 중)

| 도구 | 질문 예시 |
|---|---|
| `check_hosting` | "3MW 되나?" |
| `find_max_capacity` | "얼마까지 가능해?" |
| `get_voltage_profile` | "이 피더 전압 상태 보여줘" |
| `compare_locations` | "5번이랑 8번 중 어디가 나아?" |

### 정해야 할 것

어떤 LLM을 쓸지. 여기서 성능 상한이 결정되는데 나중에 바꾸면 다시 만들어야 함.

- 상용 API — Tool Use 품질은 가장 좋음. 비용과 키 관리 필요
- 오픈 모델 로컬 — 무료. 한국어 파라미터 추출이 약할 수 있음
- 혼합 — 추출은 상용, 서술은 로컬

---

## 7. 평가셋

위 6번을 만들어도 "잘 되는 것 같다"로 끝나면 의미 없음.

- [ ] 질문 50개 + 정답 JSON
- [ ] 측정 지표 정하기 — 파라미터 추출 정확도, 환각률, 도구 선택 정확도, 응답 시간

이게 없으면 성능을 숫자로 말할 수 없음.

---

## 8. 환경 정리

- [ ] `docker-compose.yml` — Neo4j 따로 설치하는 게 진입 장벽
- [ ] Neo4j 없이 CIM XML만으로 돌아가는 경로
      (`pv_analysis.py`가 이미 독립 실행되니 그 방식을 확장)
- [ ] README에 완성도 구분 명시 —
      전선 임피던스는 규격 기반, 부하값과 8760 프로파일은 임의값

---

## 시연 시나리오 (목표)

6번까지 되면 이 네 마디가 매끄럽게 돌아가야 함.

```
1. "DL2 피더 상태 보여줘"
2. "8번 구간에 3MW 태양광 되나?"
3. "그럼 얼마까지 되지?"
4. "5번 구간이면?"
```
